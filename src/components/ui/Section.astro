---
import { twMerge } from 'tailwind-merge'

interface Props {
  className?: string
  id?: string
}

const { className, id } = Astro.props
---

<!-- <section class=`mb-20 ${className}`>
  <slot>
</section> -->

<section id={id} class={twMerge('mb-20', className)}>
  <slot>
</section>